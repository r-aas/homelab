apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base

# Local overlay patches can be added here
# Example: custom secrets, resource limits, etc.

# patchesStrategicMerge:
#   - secrets-patch.yaml
